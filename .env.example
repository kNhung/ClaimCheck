# Example environment variables for ClaimCheck
# Copy this to a file named `.env` (and add `.env` to .gitignore) and fill in your keys.

# Serper / SerpAPI key (for web search)
SERPER_API_KEY=1b6835e378aa4dd815df3a6dc6bc9b68a073f95c

# Config model name and max actions
FACTCHECK_MODEL_NAME=qwen2.5:0.5b
FACTCHECKER_MAX_ACTIONS=2